import 'package:flutter/material.dart';

class RacTeacher extends StatelessWidget {
  const RacTeacher({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('RAC'),
      ),
      body: SafeArea(
        child: ListView(
          children: [
            Column(
              children: [],
            ),
          ],
        ),
      ),
    );
  }
}
